import { ControlWrapDirective } from './control-wrap.directive';

describe('ControlWrapDirective', () => {
  it('should create an instance', () => {
    const directive = new ControlWrapDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
